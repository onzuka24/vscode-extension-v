# 概要

<!-- 何を変えたか。関連するイシューがあれば "Closes #12" のように書いてください。 -->

## 変更した挙動

<!--
ふるまいが変わる場合は、キー列と結果で書いてください。
テストの記述をそのまま貼るのがいちばん確実です。

    run('hello world', 'ge', { cursor: pos(0, 6) }).at  // '0:4'
-->

## 確認

- [ ] `npm run lint` `npm run typecheck` `npm test` が通る
- [ ] ふるまいを変えた場合、それを示すテストを追加した
- [ ] `src/core/` に `vscode` の import を持ち込んでいない
- [ ] キーバインドを増やした場合、`when` 句に `editorTextFocus` を入れた

<!--
下の2つは構造テストが自動で検査するので、通っていれば問題ありません。
手元で確認したい場合は npm test を実行してください。
-->

## 手動で確認したこと

<!--
自動テストで追えない範囲を変更した場合に記入してください。該当しなければ削除して構いません。

- 日本語入力（Insert モードでの変換・確定）
- ターミナルや検索欄にフォーカスがあるときのキー干渉
- CRLF のファイルでの編集
-->
